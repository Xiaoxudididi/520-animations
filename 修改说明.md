# 520浪漫动画 - 修改说明

每个动画文件开头都有详细的注释标注了可定制参数位置。以下是每个动画的快速修改指南：

---

## 1. 手写情书·翻页动画 (`01-手写情书-翻页动画.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 收信人昵称 | `【参数1】` | 修改 `recipientName` 变量的值 |
| 情书全文 | `【参数2】` | 修改 `loveLetterLines` 数组，每行一个字符串 |
| 写信日期 | `【参数3】` | 修改 `letterDate` 变量的值 |
| 邮戳文字 | `【参数4】` | 修改 `stampText` 变量的值 |
| 信纸背景 | `【参数5】` | 修改CSS中 `:root` 的 `--paper-bg` 等变量，或取消注释白色/粉色主题的CSS |

**防转卖元素位置**：
- 邮戳水印：`stampWatermark` 元素，JS中 `wmText` 变量
- 底部隐藏文字：`antiResaleFooter` 元素，颜色与body背景相同
- 防转卖编码：JS顶部 `ANTI_RESALE_CODE` 和 `ANTI_RESALE_NAME`

---

## 2. 爱心拼图·回忆相册 (`02-爱心拼图-回忆相册.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 16张照片URL | `【参数1】` | 修改 `photos` 数组中的URL，支持任何图片链接 |
| 最终告白文字 | `【参数2】` | 修改 `finalMessage` 变量 |
| 拼图完成时长 | `【参数3】` | 修改 `assembleDuration` 变量（单位毫秒） |
| 背景颜色 | `【参数4】` | 修改CSS `:root` 的 `--bg` 变量，或取消注释 `.pink-theme`/`.blue-theme` |

**防转卖元素位置**：
- 中央水印：`centerWatermark` 元素，显示 "XXX专属定制"
- 照片1px标记：每个拼图块 `piece-marker` 类
- 散落位置种子：JS中 `SEED` 变量

---

## 3. 烟花告白·夜空绽放 (`03-烟花告白-夜空绽放.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 告白语 | `【参数1】` | 修改 `confessionText` 变量（最多10个字） |
| 烟花主色调 | `【参数2】` | 修改 `mainHue` 变量（pink/red/purple/blue） |
| 背景音乐URL | `【参数3】` | 修改 `musicUrl` 变量，填入音乐文件URL |
| 结尾祝福文字 | `【参数4】` | 修改 `blessingText` 变量 |

**防转卖元素位置**：
- 烟花颜色序列：JS中 `CODE_COLORS` 数组，由编码派生
- 爱心烟花雨特殊标记：`ANTI_RESALE_NAME` 影响颜色
- Canvas右下角印记：渲染循环末尾的 `fillText`

---

## 4. 心跳轨迹·爱情地图 (`04-心跳轨迹-爱情地图.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 3个地点经纬度 | `【参数1】` | 修改 `locations` 数组的 lat/lng/name |
| 地点日期和事件 | `【参数2】` | 修改 `locationDetails` 数组的 date/desc |
| 心跳速度 | `【参数3】` | 修改 `heartbeatSpeed` 变量（1-5，数字越大越快） |
| 地图风格 | `【参数4】` | 修改 `mapStyle` 变量（standard/satellite/night） |
| 结尾告白文字 | `【参数5】` | 修改 `finalMessage` 变量 |

**防转卖元素位置**：
- 地图底部文字：`footerTextEl` 元素，文本由分散变量 `_p1`-`_p4` 拼接
- 标记颜色：`MARKER_COLORS` 数组由编码派生
- 心电图颜色：`ecgColorHue` 变量

---

## 5. 樱花飘落·时光胶囊 (`05-樱花飘落-时光胶囊.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 胶囊刻字 | `【参数1】` | 修改 `capsuleEngravingText` 变量 |
| 3-5张照片URL | `【参数2】` | 修改 `photos` 数组 |
| 情话气泡内容 | `【参数3】` | 修改 `loveNotes` 数组 |
| 樱花颜色 | `【参数4】` | 修改CSS `:root` 的樱花颜色变量，或取消注释白色/紫色主题 |

**防转卖元素位置**：
- 胶囊底部微缩文字：`capsuleSubEngraving` 元素
- 气泡位置随机种子：JS中 `rng()` 函数由 `SEED` 驱动
- 底部隐藏文字：`antiFooter` 元素

---

## 6. 密码告白·惊喜解锁 (`06-密码告白-惊喜解锁.html`)

| 参数 | 搜索关键词 | 修改方法 |
|------|-----------|---------|
| 解锁密码 | `【参数1】` | 修改 `CORRECT_PASSWORD` 变量 |
| 错误提示文字 | `【参数2】` | 修改 `errorMessages` 数组（最多3条） |
| 解锁后告白文字 | `【参数3】` | 修改 `confessionLines` 数组 |
| 1-3张照片URL | `【参数4】` | 修改 `successPhotos` 数组 |
| 背景音乐URL | `【参数5】` | 修改 `musicUrl` 变量 |

**防转卖元素位置**：
- 底部"密码仅XXX知道"：`antiFooterUnlock` 元素
- 爱心爆炸颜色：由 `ANTI_RESALE_CODE` 派生
- 密码默认值：`defaultPassword` 函数

---

## 手机端布局说明

所有6个动画已针对手机屏幕（375-414px宽）进行优化：

- **全屏沉浸式**：所有页面使用 `100vw` / `100dvh` 全屏布局，无滚动条
- **自适应单位**：字体使用 `clamp()` 函数，元素宽度使用 `vw` / `vh` 或 `min()` 函数
- **安全区域**：全面适配iPhone刘海屏/安卓挖孔屏，使用 `env(safe-area-inset-*)` 
- **触摸优化**：所有按钮最小44px触摸区域，`-webkit-tap-highlight-color: transparent` 消除点击高亮
- **防误触**：禁用长按菜单 (`-webkit-touch-callout: none`)，禁用页面弹性滚动 (`overscroll-behavior: none`)
- **键盘适配**：密码动画专门处理了iOS键盘弹出/收起时的页面偏移
- **屏幕旋转**：所有动画监听 `orientationchange` 事件，旋转后自动重绘

## 通用注意事项

1. **微信浏览器兼容**：所有动画均使用标准HTML5/CSS3/ES5语法，兼容微信内置浏览器
2. **无外部依赖**：除第4个动画使用Leaflet.js CDN（地图必需）和第1个使用Google Fonts（字体）外，无其他外部资源
3. **加载速度**：所有代码已精简优化，文件大小均在13-17KB
4. **测试方法**：直接用微信发送.html文件，在微信中打开即可查看效果
5. **照片替换**：将 `data:image/svg+xml,...` 替换为真实图片URL即可显示真实照片
6. **手机预览**：在电脑浏览器中打开后按F12切换到手机模式可预览移动端效果
